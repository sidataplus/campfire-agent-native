<% @page_title = "New integration credential" %><section class="agent-workspace"><h1>Save this token now</h1><p>For <%= @profile.user.name %>. It is displayed only in this response. Store it in a private token file or your runtime secret store, not in chat.</p><pre><%= @token %></pre><p>This response is not cached. The database retains only the token digest.</p><%= link_to "Return to integrations", "/agent/admin" %></section>
